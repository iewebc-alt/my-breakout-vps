#!/bin/bash

# Путь к папке с архивами
BACKUP_DIR="/var/www/backups/full_versions"
# Текущая рабочая директория
TARGET_DIR=$(pwd)

# Проверка существования папки с бэкапами
if [ ! -d "$BACKUP_DIR" ]; then
    echo "❌ Ошибка: Папка с архивами не найдена по пути $BACKUP_DIR"
    exit 1
fi

# Получаем список ZIP файлов
cd "$BACKUP_DIR" || exit
files=(*.zip)

# Проверка, есть ли файлы в папке
if [ ${#files[@]} -eq 0 ] || [ "${files[0]}" == "*.zip" ]; then
    echo "📂 В папке $BACKUP_DIR нет доступных архивов (.zip)"
    exit 1
fi

echo "-------------------------------------------------------"
echo "📜 Список доступных версий проекта для восстановления:"
echo "-------------------------------------------------------"

# Выводим список файлов с номерами
for i in "${!files[@]}"; do
    printf "[%2d] %s\n" "$((i+1))" "${files[$i]}"
done

echo "-------------------------------------------------------"
echo "🔢 Введите номер архива для восстановления (или 'q' для выхода):"
read -r choice

# Выход, если пользователь ввел 'q'
if [[ "$choice" == "q" ]]; then
    echo "👋 Отмена восстановления."
    exit 0
fi

# Проверка корректности ввода
if ! [[ "$choice" =~ ^[0-9]+$ ]] || [ "$choice" -lt 1 ] || [ "$choice" -gt "${#files[@]}" ]; then
    echo "❌ Ошибка: Неверный номер."
    exit 1
fi

# Определяем выбранный файл
SELECTED_FILE="${files[$((choice-1))]}"

echo -e "\n⚠️  ВНИМАНИЕ: Файлы в $TARGET_DIR будут перезаписаны файлами из архива."
echo "Продолжить восстановление из $SELECTED_FILE? (y/n)"
read -r confirm

if [[ "$confirm" != "y" ]]; then
    echo "❌ Восстановление отменено."
    exit 0
fi

# Возвращаемся в рабочую папку и распаковываем
cd "$TARGET_DIR" || exit
echo "📦 Распаковка $SELECTED_FILE..."

# -o означает overwrite (перезаписать без вопросов)
unzip -o "$BACKUP_DIR/$SELECTED_FILE" -d "$TARGET_DIR"

if [ $? -eq 0 ]; then
    echo "-------------------------------------------------------"
    echo "✅ Проект успешно восстановлен до версии: $SELECTED_FILE"
    echo "🚀 Не забудьте запустить ./update_dev.sh для обновления Docker-контейнера."
    echo "-------------------------------------------------------"
else
    echo "❌ Ошибка при распаковке архива."
fi
